"use strict";

// 1. Arrow function tanpa argument
const returnHello = () => "Hello";

// 2. Arrow function dengan satu argument
const doubleNumber = (x) => x * 2;

function area(x, y) {
  return x * y;
}

// 3. Arrow function lebih dari satu argument.

const areaArrow = (x, y) => x * y;

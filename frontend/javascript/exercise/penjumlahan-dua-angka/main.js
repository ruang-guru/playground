// Diberikan array bilangan bulat nums dan target dengan tipe integer, kembalikan indeks dari dua angka sedemikian rupa sehingga jika dijumlahkan sama dengan target.
// Kita dapat berasumsi bahwa setiap input akan memiliki tepat satu solusi, dan tidak boleh menggunakan elemen yang sama dua kali.
// Kita dapat mengembalikan jawabannya dalam urutan apa pun.

// Contoh 1
// Input: nums = [2,7,11,15], target = 9
// Output: [0,1]
// Karena nums[0] + nums[1] == 9, kembalikan indeks [0, 1].
//
// Contoh 2
// Input: nums = [3,2,4], target = 6
// Output: [1,2]
// Karena nums[1] + nums[2] == 6, kembalikan indeks [1, 2].
//
// Contoh 3
// Input: nums = [3,3], target = 6
// Output: [0,1]
// Karena nums[0] + nums[1] == 6, kembalikan indeks [0, 1].

function twoSum(nums, target) {
  // Tulis kode di sini
  // TODO: answer here
};